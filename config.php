<?php
/**
 * Everything about the site that isn't a picture.
 *
 * Edit this file, save it, reload the page. There is no admin panel and no
 * database — this file and data/artworks.json are the whole of the content.
 */

return [

    // Shown across the top of the page, on the opening screen, and in the
    // browser tab.
    'artistName' => 'TALES OF NIGHTMARE',

    // A quiet line under the name. Leave as '' to hide it.
    'tagline'    => 'Drawings by Inam',

    // Appears below the work, set in italics. Leave a blank line between
    // paragraphs. Set to '' to hide the section entirely.
    'statement'  => "These start as things I see with my eyes shut.\n\n"
                  . "Some of them are tender and some of them are not, and I have given up "
                  . "sorting them into two piles. A dream never tells you which kind it is "
                  . "until you are already inside it.\n\n"
                  . "Most are drawn in one line, without lifting, because that is how they "
                  . "arrive — all at once, or not at all.",

    // Contact. Either may be '' to leave it off the footer.
    'email'      => 'twistedmock007@gmail.com',

    // Full URL or bare handle — both work.
    'instagram'  => 'https://www.instagram.com/talesofn1ghtmares',

    // The small print at the very bottom.
    'footerNote' => 'All works © Inam. Please do not reproduce without permission.',

    // Where the site actually lives. Used only for the link preview that
    // appears when somebody shares it — those need a full URL, not a relative
    // one. Leave '' and the preview image is simply left off.
    'siteUrl'    => 'https://twistedmock.github.io/talesofnightmare/',
];
