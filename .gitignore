# Editor and OS litter
.DS_Store
Thumbs.db
*.swp
*~

# Nothing is written at runtime, but a stray atomic-write temp file from an
# earlier version of this site would look like content if it were committed.
data/*.tmp
